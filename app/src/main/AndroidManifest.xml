<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.will.custom_rxandroid">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".BaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.elementary.ElementaryActivity"
            android:label="@string/title_activity_base"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.map.MapActivity" />
        <activity android:name=".ui.zip.ZipActivity" />
        <activity android:name=".ui.token.TokenActivity" />
        <activity android:name=".ui.token_avanced.TokenAvancedActivity" />
        <activity android:name=".ui.cache.CacheActivity" />
        <activity android:name=".ui.time.TimeActivity" />
        <activity android:name=".ui.some_operator.OperatorActivity" />
        <activity android:name=".ui.subject.SubjectActivity" />
        <activity android:name=".ui.custom_view.CustomViewActivity"></activity>
    </application>

</manifest>